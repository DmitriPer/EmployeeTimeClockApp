export { isCurrentMonth, isCurrentMonthDate } from '@app/shared';

/** Alias for callers that pass an ISO clockInAt string directly. */
export { isCurrentMonth as isCurrentMonthEntry } from '@app/shared';
